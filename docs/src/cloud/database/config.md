# Cloud Database Config

The `cloud.database.config` resource handles configuration entries for a managed cloud database.

## Get Config Entry

- No parameters are currently available in the SDK implementation.

```py
from infomaniak import Client

client = Client(token="<token>")
entry: None = client.cloud.database.config.get()
```

`None`:

- No return object is currently available in the SDK implementation.

## List Config Entries

- No parameters are currently available in the SDK implementation.

```py
from infomaniak import Client

client = Client(token="<token>")
entries: None = client.cloud.database.config.list()
```

`None`:

- No return object is currently available in the SDK implementation.

## Create Config Entry

- No parameters are currently available in the SDK implementation.

```py
from infomaniak import Client

client = Client(token="<token>")
created: None = client.cloud.database.config.create()
```

`None`:

- No return object is currently available in the SDK implementation.

## Update Config Entry

- No parameters are currently available in the SDK implementation.

```py
from infomaniak import Client

client = Client(token="<token>")
updated: None = client.cloud.database.config.update()
```

`None`:

- No return object is currently available in the SDK implementation.

## Remove Config Entry

- No parameters are currently available in the SDK implementation.

```py
from infomaniak import Client

client = Client(token="<token>")
removed: None = client.cloud.database.config.remove()
```

`None`:

- No return object is currently available in the SDK implementation.
