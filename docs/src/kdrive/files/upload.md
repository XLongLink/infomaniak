# kDrive Files Upload

Resource for direct upload operations.

## Available methods

- `client.kdrive.files.upload.upload()`
- `client.kdrive.files.upload.cancel_by_path()`

::: warning
All methods are currently unimplemented and raise `NotImplementedError`.
:::
