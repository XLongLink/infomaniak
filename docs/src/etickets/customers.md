# eTickets Customers

Resource file: `infomaniak/resources/etickets/customers/__init__.py`.

## Methods

- `logs(customer_id: int)` → `GET /2/etickets/customers/{customer_id}/logs`

## Status

`logs()` is currently a placeholder and raises `NotImplementedError`.
