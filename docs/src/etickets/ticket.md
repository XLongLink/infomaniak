# eTickets Ticket

Resource file: `infomaniak/resources/etickets/ticket.py`.

## Methods

- `update()`

## Status

`update()` is currently a placeholder and raises `NotImplementedError`.
The exact HTTP signature still needs to be confirmed from `openapi.json`.
