# eTickets Survey Answers Tickets

Resource file: `infomaniak/resources/etickets/surveys/answers/tickets.py`.

## Methods

- `list()` → `GET /2/etickets/surveys/answers/tickets`
- `update()` → `PATCH /2/etickets/surveys/answers/tickets`

## Status

Both methods are currently placeholders and raise `NotImplementedError`.
