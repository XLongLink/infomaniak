# eTickets Survey Answers Passes

Resource file: `infomaniak/resources/etickets/surveys/answers/passes.py`.

## Methods

- `list()` → `GET /2/etickets/surveys/answers/passes`
- `update()` → `PATCH /2/etickets/surveys/answers/passes`

## Status

Both methods are currently placeholders and raise `NotImplementedError`.
