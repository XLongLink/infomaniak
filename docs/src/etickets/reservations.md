# eTickets Reservations

Resource file: `infomaniak/resources/etickets/reservations.py`.

## Methods

- `display_by_id(reservation_id: int)` → `GET /2/etickets/reservation/{reservation_id}`
- `display_by_uuid(reservation_uuid: str)` → `GET /2/etickets/reservation/{reservation_uuid}`

## Status

Both methods are currently placeholders and raise `NotImplementedError`.
