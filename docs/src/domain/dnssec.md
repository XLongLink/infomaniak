# Domain DNSSEC

This page documents the `infomaniak/resources/domain/dnssec.py` resource.

## Methods

- `check()`
- `enable()`
- `disable()`

## Status

All methods are currently placeholders and raise `NotImplementedError`.
