# Domain Order

This page documents the `infomaniak/resources/domain/order.py` resource.

## Methods

- `check()`
- `create()`
- `transfer()`

## Status

All methods are currently placeholders and raise `NotImplementedError`.
